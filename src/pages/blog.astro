---
import { getCollection } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';

const posts = await getCollection('posts');
const sortedPosts = posts.sort((a, b) => b.data.date.getTime() - a.data.date.getTime());

const df = new Intl.DateTimeFormat('ko-KR', {
  year: '2-digit',
  month: '2-digit',
  day: '2-digit',
});

function formatDate(d: Date): string {
  // "26. 01. 11." -> "26.01.11"
  return df
    .format(d)
    .replaceAll('. ', '.')
    .replaceAll(' ', '')
    .replace(/\.$/, '');
}
---

<BaseLayout title="Blog">
  <div class="container mx-auto px-4 py-6 sm:py-8">
    <div class="max-w-3xl mx-auto">
      <h1 class="text-lg sm:text-xl font-bold text-text-primary mb-3">
        Blog
      </h1>

      <!-- 밀도/가독성 우선: 제목 + 날짜만. 잘림/요약/카드 UI 제거 -->
      <ul class="divide-y divide-border">
        {sortedPosts.map((post) => (
          <li>
            <a
              href={`/blog/${post.slug}`}
              class="group block py-2.5 sm:py-2 focus:outline-none focus-visible:ring-2 focus-visible:ring-accent focus-visible:ring-offset-2 focus-visible:ring-offset-bg-primary"
            >
              <div class="flex gap-2 sm:gap-3 items-start">
                <time class="shrink-0 text-[11px] sm:text-xs text-text-tertiary font-mono tabular-nums mt-[2px]">
                  {formatDate(post.data.date)}
                </time>

                <h2 class="min-w-0 flex-1 text-[13px] sm:text-sm font-medium leading-snug text-text-primary group-hover:text-accent transition-colors break-words">
                  {post.data.title}
                </h2>
              </div>
            </a>
          </li>
        ))}
      </ul>
    </div>
  </div>
</BaseLayout>
